# Capture-the-flag-in-deepmind-lab
This repository consits of capture the flag files from user 'kimbring2/dmlab_ctf' which are copied into deepmind lab.

There exists some duplicate files in some directories.
